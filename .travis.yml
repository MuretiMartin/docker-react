sudo : required
services:
  - docker
before_install :
   - docker build -t mmuretimartin/docker-react -f Dockerfile.dev .

script:
   - docker run mmuretimartin/docker-react npm run test -- --coverage